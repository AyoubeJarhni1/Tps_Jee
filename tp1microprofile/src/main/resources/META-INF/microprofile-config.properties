# Application Configuration
app.name=TP1 MicroProfile
app.version=1.0.0
app.description=Application MicroProfile pour la gestion de produits et clients

# Limites de l'application
app.max.products=1000
app.max.clients=500

# Fonctionnalités
app.enable.metrics=true
app.enable.health=true

# Server Configuration
server.port=9080
server.host=localhost
